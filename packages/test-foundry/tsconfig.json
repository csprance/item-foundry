{
  "compilerOptions": {
    "target": "ESNext", // Use the latest ECMAScript version for better performance
    "module": "ESNext", // Enable ECMAScript modules
    "lib": ["ESNext", "DOM"], // Include latest JavaScript and DOM library
    "strict": true, // Enable strict type-checking options
    "moduleResolution": "node", // Resolve modules in a Node.js-style
    "experimentalDecorators": true, // Enable support for decorators
    "emitDecoratorMetadata": true, // Emit metadata required by decorators
    "esModuleInterop": true, // Ensure compatibility with CommonJS modules
    "forceConsistentCasingInFileNames": true, // Ensure file names are case-sensitive
    "skipLibCheck": true, // Skip type checking of declaration files for faster builds
    "declaration": true, // Generate declaration files for TypeScript
    "outDir": "./dist", // Output directory for compiled files
    "rootDir": "./src", // Root directory for input files
    "resolveJsonModule": true, // Enable importing of .json files
    "sourceMap": true,
    // Generate source maps for debugging
    "paths": {
      "~": [
        "./src"
      ],
      "~/*": [
        "./src/*"
      ]
    }
  },
  "include": [
    "src/**/*" // Include all source files in src directory
  ],
  "exclude": [
    "node_modules", // Exclude node_modules from compilation
    "tests/**/*" // Exclude test files from compilation
  ]
}
